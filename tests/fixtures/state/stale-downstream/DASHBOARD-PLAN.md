# Dashboard Plan (fixture placeholder)

Present so the mock step's ordering gate (required read: DASHBOARD-PLAN.md) is
satisfied. Content is irrelevant to the router — only the file's existence is
checked.
